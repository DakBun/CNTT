def scrape_vn_jobs(source: str, limit: int = 100) -> None:
    """Scrape Vietnamese IT job listings from a given source.

    Args:
        source: Job board name (e.g., "itviec", "topcv").
        limit: Maximum number of job postings to fetch.

    Returns:
        None
    """
    raise NotImplementedError
